We implement the following in order:

- Run the file `generate.py`. This generates 30 datasets.

- Run the file `l0l1.py`. This generates 30 * 20 `.npy` files.

- Run the file `collect.py` to collect simulation results. This generates a file `l0l1.csv`.

- Run the file `figure.R` to generate the figure.


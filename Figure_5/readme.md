We implement the following in order:

- Run the file `generate.py`. This generates 30 datasets.

- Run the file `early_stopping.py`. This generates 30 * 11 `.npy` files.

- Run the file `collect.py` to collect simulation results. This generates a file `early_stopping.csv`.

- Run the file `figure.R` to generate the figure.

